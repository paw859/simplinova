{% comment %}
  Professional CTA Section
  Call to action for newsletter or special offer
{% endcomment %}

<section class="cta-section">
  <div class="cta-content">
    <h2>Get Exclusive Deals First</h2>
    <p>Subscribe to our newsletter for early access to new drops, exclusive discounts, and insider tips.</p>
    
    <form class="cta-form">
      <input type="email" placeholder="Enter your email" required>
      <button type="submit" class="cta-button">Subscribe</button>
    </form>
  </div>
</section>

<style>
  .cta-section {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-light) 100%);
    color: var(--color-white);
    padding: 64px 20px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .cta-section::before {
    content: '';
    position: absolute;
    top: 0;
    right: -100px;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
    pointer-events: none;
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
  }

  .cta-section h2 {
    color: var(--color-white);
    margin-bottom: var(--spacing-lg);
  }

  .cta-section p {
    color: rgba(255, 255, 255, 0.9);
    margin-bottom: var(--spacing-2xl);
    font-size: 17px;
  }

  .cta-form {
    display: flex;
    gap: var(--spacing-md);
    max-width: 400px;
    margin: 0 auto;
  }

  .cta-form input {
    flex: 1;
    padding: 12px 16px;
    border: none;
    border-radius: var(--radius-md);
    font-size: 15px;
    color: var(--color-gray-700);
  }

  .cta-form input::placeholder {
    color: var(--color-gray-400);
  }

  .cta-button {
    background: var(--color-white);
    color: var(--color-accent);
    padding: 12px 28px;
    font-weight: 600;
    border: none;
    border-radius: var(--radius-md);
    cursor: pointer;
    transition: all var(--transition-base);
    font-size: 15px;
    white-space: nowrap;
  }

  .cta-button:hover {
    background: var(--color-bg-light);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  @media (max-width: 768px) {
    .cta-section {
      padding: 48px 20px;
    }

    .cta-form {
      flex-direction: column;
    }

    .cta-button {
      width: 100%;
    }
  }
</style>
